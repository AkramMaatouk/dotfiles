# ─────────────────────────────────────────────
# Pi Agent configuration
# Preserve your exact config format here.
# This file is symlinked to ~/.config/pi-agent/config.toml
# ─────────────────────────────────────────────

# [agent]
# name    = "default"
# log_dir = "~/.local/share/pi-agent/logs"

# [tools]
# shell   = "zsh"
# editor  = "nvim"
